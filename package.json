{
  "name": "revisita",
  "version": "1.4.6",
  "description": "PWA en español para guardar revisitas por ubicación, notas y fecha.",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check:encoding": "node scripts/check-encoding.mjs .",
    "ship-check": "node scripts/khub-check.mjs .",
    "check": "npm run check:encoding && npm run check:syntax && npm test && npm run ship-check",
    "check:syntax": "node --check js/app.js && node --check js/map.js && node --check js/storage.js && node --check js/schedule-utils.js && node --check js/i18n.js && node --check js/visit-tools.js && node --check js/cloud-sync.js && node --check js/push.js && node --check cloudflare/revisita-push/worker.js && node --check sw.js && node --check js/visit-time.js && node --check cloudflare/revisita-push/web-push.js"
  },
  "engines": {
    "node": ">=18"
  }
}
